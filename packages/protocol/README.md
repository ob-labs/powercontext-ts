# @powercontext/protocol

Generated wire types, operation metadata and runtime validators.

Phase 1 lands the generation spike: `openapi-typescript` types, 52 operation
metadata rows, Ajv validators compiled from an explicit OpenAPI 3.0 conversion,
and the ADR 0001 safe-integer overlay. The package must not depend on a
database or LLM SDK.
