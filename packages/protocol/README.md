# @powercontext/protocol

Generated wire types, operation metadata, and runtime validators for all 52
operations. JSON, text, and structured errors are validated at runtime. The
only versioned overlay is `integer-safe-range.v1`.

The package must not depend on a database or LLM SDK. See
[docs/develop/generating-protocol.md](../../docs/develop/generating-protocol.md).
