# @powercontext/client

Official typed HTTP Client. First publishable product (M1).

Phase 1 only publishes the empty packable skeleton. Transport and 52 typed
methods arrive in Phase 3. Installing this package must not compile a native
addon.
