name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10.33.2
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - uses: astral-sh/setup-uv@v10
      - run: python -m pip install -r tools/contract-sync/requirements.lock.txt
      - run: pnpm install --frozen-lockfile
      - name: Checkout Python pin
        uses: actions/checkout@v7
        with:
          repository: oceanbase/powercontext
          ref: 733e4bf6b378785e76274ff07632029c699ecb09
          path: .python-pin
      - run: python conformance/runners/python/bootstrap.py --python-repo .python-pin
      - run: pnpm format
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test
      - run: pnpm license:check
      - run: pnpm generate:check
      - run: pnpm deps:licenses
      - run: pnpm deps:audit
      - run: python tools/contract-sync/sync.py --check --python-repo .python-pin
      - run: python tools/contract-sync/verify.py --python-repo .python-pin
      - run: python tools/contract-sync/test_sync.py --python-repo .python-pin
      - run: pnpm pack:smoke

  client-matrix:
    name: client-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10.33.2
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm
      - name: Install Client test toolchain without Runtime native builds
        run: pnpm install --frozen-lockfile --config.engine-strict=false --ignore-scripts
      - run: pnpm test:client

  runtime-matrix:
    name: runtime-matrix
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10.33.2
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test:runtime

  smoke:
    name: smoke
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10.33.2
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - run: python -m pip install -r tools/contract-sync/requirements.lock.txt
      - run: pnpm install --frozen-lockfile
      - name: Checkout Python pin
        uses: actions/checkout@v7
        with:
          repository: oceanbase/powercontext
          ref: 733e4bf6b378785e76274ff07632029c699ecb09
          path: .python-pin
      - run: pnpm deps:licenses
      - run: pnpm build
      - run: pnpm test:sqlite-spike
      - run: python tools/contract-sync/sync.py --check --python-repo .python-pin
      - run: python tools/contract-sync/verify.py --python-repo .python-pin
      - run: python tools/contract-sync/test_sync.py --python-repo .python-pin
      - run: pnpm pack:smoke

  oracle-smoke:
    name: oracle-smoke
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - uses: astral-sh/setup-uv@v10
      - run: python -m pip install -r tools/contract-sync/requirements.lock.txt
      - name: Checkout Python pin
        uses: actions/checkout@v7
        with:
          repository: oceanbase/powercontext
          ref: 733e4bf6b378785e76274ff07632029c699ecb09
          path: .python-pin
      - run: python conformance/runners/python/bootstrap.py --python-repo .python-pin
      - run: python conformance/runners/python/run.py --check
